---
phase: 05-tmux-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/sessions.ts
  - src/sessions/session-store.ts
  - src/hooks/handlers.ts
  - src/utils/install-hooks.ts
  - src/control/input-manager.ts
  - src/bot/bot.ts
  - src/index.ts
autonomous: true
requirements: [CLN-01, CLN-02, CLN-03]

must_haves:
  truths:
    - "The hook installer no longer references capture-tmux-pane.sh or installs any tmux-related hooks"
    - "TextInputManager contains no tmux send-keys or paste-buffer code paths"
    - "Session types and session store have no tmuxPane field"
    - "The project builds cleanly with zero TypeScript errors"
    - "grep -r tmux src/ returns zero matches"
  artifacts:
    - path: "src/types/sessions.ts"
      provides: "SessionInfo without tmuxPane field"
      contains: "interface SessionInfo"
    - path: "src/sessions/session-store.ts"
      provides: "SessionStore without updateTmuxPane method"
    - path: "src/bot/bot.ts"
      provides: "SDK-only text handler with no tmux fallback"
      contains: "sdkInputManager.send"
    - path: "src/utils/install-hooks.ts"
      provides: "Hook installer with HTTP hooks only, no command hook"
    - path: "src/index.ts"
      provides: "Entry point with no TextInputManager, no tryReadTmuxPane, no tmux file I/O"
  key_links:
    - from: "src/bot/bot.ts"
      to: "src/sdk/input-manager.ts"
      via: "sdkInputManager.send() as sole input path"
      pattern: "sdkInputManager\\.send"
    - from: "src/index.ts"
      to: "src/bot/bot.ts"
      via: "createBot() without inputManager parameter"
      pattern: "createBot\\(config.*sessionStore.*approvalManager.*sdkInputManager\\)"
    - from: "src/utils/install-hooks.ts"
      to: "~/.claude/settings.json"
      via: "HTTP-only SessionStart hook config"
      pattern: "SessionStart.*http"
---

<objective>
Remove all tmux-related code from the codebase: the TextInputManager class, tmux pane capture hook, tmuxPane session field, and all tmux fallback logic. After this plan, the SDK resume path is the sole input mechanism with no dead code or orphaned imports.

Purpose: Eliminate dead code paths left over from the Phase 4 SDK migration, satisfying CLN-01/02/03
Output: Clean codebase with zero tmux references in src/, passing TypeScript build
</objective>

<execution_context>
@/home/cryptobot/.claude/get-shit-done/workflows/execute-plan.md
@/home/cryptobot/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-tmux-cleanup/05-RESEARCH.md

Reference: The research file contains a complete line-by-line removal manifest for all 7 files. Follow it precisely.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove tmux data layer (types, store, handlers, hook installer)</name>
  <files>
    src/types/sessions.ts
    src/sessions/session-store.ts
    src/hooks/handlers.ts
    src/utils/install-hooks.ts
  </files>
  <action>
Remove all tmux-related type definitions, store methods, handler initializations, and hook installer code. Follow the research manifest precisely:

**src/types/sessions.ts:**
- Remove lines 43-44: the `tmuxPane: string | null` field and its JSDoc comment from `SessionInfo` interface

**src/sessions/session-store.ts:**
- Remove lines 171-178: the entire `updateTmuxPane()` method
- Remove line 231: `tmuxPane: session.tmuxPane ?? null` from the `load()` method. Extra fields in persisted JSON are harmlessly ignored by TypeScript structural typing.

**src/hooks/handlers.ts:**
- Remove line 74: `tmuxPane: null` from the `ensureSession()` method's SessionInfo literal
- Remove line 253: `tmuxPane: null` from `handleSessionStart()` method's SessionInfo literal

**src/utils/install-hooks.ts:**
- Remove lines 44-61: the entire tmux script generation block (tmuxScript const, hooksDir const, mkdirSync for hooksDir, scriptPath, writeFileSync for script, chmodSync)
- Remove line 71: `{ type: 'command', command: scriptPath, timeout: 5 }` from the SessionStart hooks array, leaving only the HTTP hook entry
- Remove `chmodSync` from the import on line 1 (still keep readFileSync, writeFileSync, existsSync, mkdirSync -- they are used elsewhere)
- Remove `mkdirSync` for hooksDir ONLY (the mkdirSync for claudeDir on line 21 must stay)

After these edits, the SessionStart hook config should look like:
```typescript
SessionStart: [
  {
    matcher: '',
    hooks: [
      { type: 'http', url: `${baseUrl}/hooks/session-start`, timeout: 10 },
    ],
  },
],
```
  </action>
  <verify>npx tsc --noEmit 2>&1 | head -20 (expect errors about bot.ts and index.ts referencing removed fields -- these are fixed in Task 2)</verify>
  <done>tmuxPane field gone from SessionInfo, updateTmuxPane method gone from SessionStore, tmuxPane initialization gone from handlers, tmux script generation and command hook gone from install-hooks</done>
</task>

<task type="auto">
  <name>Task 2: Remove tmux consumers (TextInputManager, bot fallback, index wiring)</name>
  <files>
    src/control/input-manager.ts
    src/bot/bot.ts
    src/index.ts
  </files>
  <action>
Remove all tmux consumer code. Follow the research manifest precisely:

**src/control/input-manager.ts:**
- DELETE THE ENTIRE FILE. The TextInputManager class is fully replaced by SdkInputManager.

**src/bot/bot.ts:**
- Remove line 8: `import type { TextInputManager } from '../control/input-manager.js';`
- Remove `inputManager: TextInputManager` parameter from `createBot()` function signature (line 32). The remaining parameters should be: `config, sessionStore, approvalManager, sdkInputManager`
- Rewrite lines 177-248: Replace the SDK-first-with-tmux-fallback text handler with an SDK-only handler. The new handler should:
  1. Keep the existing guard clauses (threadId check, session lookup, active check, command prefix check)
  2. Keep the quoted message context logic (reply_to_message)
  3. Call `sdkInputManager.send(session.sessionId, text, session.cwd)` (same as now)
  4. On success (`result.status === 'sent'`): react with zap emoji (same as now)
  5. On failure: reply with error message directly -- NO tmux fallback. Use:
     ```typescript
     await ctx.reply(
       `\u274C Failed to send input: ${result.error}`,
       { message_thread_id: threadId, reply_to_message_id: ctx.message.message_id },
     );
     ```
  6. Remove ALL references to `inputManager` (the TextInputManager variable)

**src/index.ts:**
- Remove `readFileSync, unlinkSync, existsSync` from the `node:fs` import on line 2 (the `join` import from `node:path` on line 1 stays). After removal, the `node:fs` import line should be deleted entirely since no fs functions remain needed in index.ts.
- Remove line 24: `import { TextInputManager } from './control/input-manager.js';`
- Remove lines 62-77: the entire `tryReadTmuxPane()` function
- Remove line 105: `const inputManager = new TextInputManager();`
- Remove `inputManager` from the `createBot()` call on line 109. The call should become: `createBot(config, sessionStore, approvalManager, sdkInputManager)`
- Remove lines 267-272: the tmux pane capture block in `onSessionStart` callback (the `tryReadTmuxPane` call and `sessionStore.updateTmuxPane` usage)
- Remove line 297: `inputManager.cleanup(session.sessionId);` from `onSessionEnd`
- Remove lines 299-300: the tmux pane file cleanup (`unlinkSync`) in `onSessionEnd`
- Remove lines 345-352: the lazy tmux pane capture block in `onPreToolUse` callback (the `if (!session.tmuxPane)` block with `tryReadTmuxPane`)

After all edits, run verification:
```bash
npx tsc --noEmit && echo "BUILD OK"
grep -r "tmux" src/ && echo "TMUX REFS FOUND" || echo "NO TMUX REFS"
grep -r "input-manager" src/ | grep -v "sdk/input-manager" && echo "OLD INPUT-MANAGER REFS" || echo "CLEAN"
npm run build
```

All four checks must pass: TypeScript compiles, zero tmux references in src/, no references to old input-manager (only sdk/input-manager), build succeeds.
  </action>
  <verify>npx tsc --noEmit && grep -rn "tmux" src/ | grep -c . | xargs -I{} test {} -eq 0 && npm run build</verify>
  <done>src/control/input-manager.ts deleted, bot.ts has SDK-only text handler with no tmux fallback, index.ts has no TextInputManager/tryReadTmuxPane/tmux file I/O, project builds with zero errors and zero tmux references in src/</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` exits 0 (no TypeScript errors)
2. `grep -r "tmux" src/` returns zero matches
3. `grep -r "input-manager" src/` only matches `sdk/input-manager.ts` references
4. `npm run build` succeeds
5. `ls src/control/input-manager.ts` fails (file deleted)
6. `grep -c "tmuxPane" src/types/sessions.ts` returns 0
7. `grep -c "chmodSync" src/utils/install-hooks.ts` returns 0
8. `grep -c "TextInputManager" src/bot/bot.ts src/index.ts` returns 0
</verification>

<success_criteria>
- Zero TypeScript compilation errors
- Zero occurrences of "tmux" in any file under src/
- src/control/input-manager.ts no longer exists
- SessionInfo interface has no tmuxPane field
- createBot() takes 4 parameters (config, sessionStore, approvalManager, sdkInputManager)
- Text handler in bot.ts uses only sdkInputManager with direct error reporting (no fallback)
- install-hooks.ts SessionStart config has only HTTP hook (no command hook)
- npm run build produces working dist/ output
</success_criteria>

<output>
After completion, create `.planning/phases/05-tmux-cleanup/05-01-SUMMARY.md`
</output>
